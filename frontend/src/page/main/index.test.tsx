import { render, screen } from '@testing-library/react';
import Main from './';

describe('App', () => {
  it('should have headline', () => {
    expect(1).toBe(1);
  });
});

test('테스트 설명', () => {
  expect(2).toBe(2);
});
